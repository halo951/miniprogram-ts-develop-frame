import { ComponentOptions, component } from "@/library/lib";

/**
 * Component - $${name}
 * @create $${date}
 * @author $${author}
 * $${desc}
 */
@component
export default class FILE_BASIC_NAME_UPPER extends ComponentOptions {
  // 导出 Component
}
