import { PageOptions, page } from "@/library/lib";

/**
 * Page - $${name}
 * @create $${date}
 * @author $${author}
 * $${desc}
 */
@page
export default class $$FILE_BASIC_NAME_UPPER extends PageOptions {
  // 导出page
}
